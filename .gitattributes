# Normalize line endings so the repo is clean across Windows dev and Linux prod.
* text=auto eol=lf

# Binary / generated — never touch.
*.png binary
*.jpg binary
*.ico binary
package-lock.json -diff
